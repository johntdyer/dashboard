<%= form_for(@partition_platform) do |f| %>
  <% if @partition_platform.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@partition_platform.errors.count, "error") %> prohibited this partition_platform from being saved:</h2>

      <ul>
      <% @partition_platform.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="actions"> 
    <%= semantic_form_for @partition_platform do |f| %>
      <%= f.input :network, :label=>"Network: "%> 
      <%= f.input :ppid, :label=>"PPID: " %>

      <%= f.buttons %>
    <% end %>
 
  </div>
<% end %>

<h1>Listing Phono Gateways</h1>

<div id="browsers">
  <table cellpadding="0" cellspacing="0" border="0" class="display" id="example">
      <THEAD>
        <th>Hostname</th>
        <th>Description</th>
        <th></th>
      </THEAD>

      <% @phonos.each do |phono| %>
      <tr>     
        <td><%= phono.hostname %></td>
        <td><%= phono.description %></td>

        <td><%= link_to raw("#{image_tag("icons/PNG-24/Info.png")}"), phono %>
          <% if current_user.try(:admin?) %>
            <%= link_to raw("#{image_tag("icons/PNG-24/Modify.png")}"), edit_phono_path(browser) %>
            <%= link_to raw("#{image_tag("icons/PNG-24/Delete.png")}"), phono, confirm: 'Are you sure?', method: :delete %></td>
          <%end%>
      </tr>
      <% end %>
    </table>
  <br />
  </div> 
  <% if current_user.try(:admin?) %>
    <%= link_to 'New Phono', new_phono_path %>
  <%end%>
